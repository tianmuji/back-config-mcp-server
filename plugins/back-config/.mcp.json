{
  "back-config": {
    "command": "npx",
    "args": ["-y", "--registry", "https://npm.intsig.net/", "@camscanner/back-config-mcp-server@latest"],
    "env": {
      "OPERATE_BASE_URL": "https://operate.intsig.net"
    }
  }
}
